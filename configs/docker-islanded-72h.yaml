# Container path overlay for the simulation-only EMS demonstration.
extends: islanded-baseline-72h.yaml

scenario:
  name: islanded_72h_2026-01-15
  description: Containerized simulation-only EMS replay; no live telemetry or hardware actuation.

digital_twin:
  measurements:
    load:
      file: /data/processed/demand_15min_weekly_seasonal_reconstruction_candidate.csv
    pv:
      file: /data/processed/pv_15min_chronos_reconstruction_candidate.csv

forecast:
  enabled: true
  strict_cache: false
  service_url: http://host.docker.internal:8000
  horizon_hours: 24
  target_frequency_hours: 1.0
  issue_frequency_hours: 1.0
  refresh_each_step: true
